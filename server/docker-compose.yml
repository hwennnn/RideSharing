version: '3.9'
services:
  web:
    build:
      context: ./
      target: dev
    volumes:
      - ./:/app
    command: npm run start
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*